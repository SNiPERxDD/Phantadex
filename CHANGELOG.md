# Changelog

All notable changes to this project will be documented in this file.

## Phantadex / Coursera

- **[2026-08-24 17:46:19 +0530]** | Stopped a course tree relocating the archive it was only reading. The read-only manager returns before it creates a directory, migrates a ledger or takes the lock -- but the legacy *directory* migration ran above that guard, and it makes one directory and moves another. So on a machine carrying an archive from before the rename, `pdex dex` moved the user's files to answer a question about them, which is the one thing the read-only path exists to promise it will not do, and its own comment claimed it already did. The migration now runs below the guard with the rest of the run's side effects | Files: `phantadex/course_manager.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 17:46:19 +0530]** | Stopped a half-read sidebar being announced as a finished course. `get_completion_status` catches an exception around the whole scan and returns whatever it had collected, so a row going stale mid-read yields a partial dict -- and the unfinished list counts only rows explicitly marked incomplete, so a reading cut short at the top lists nothing unfinished at all. A run over a barely-started course then ended on "every item in the course map is already complete": the one report a run must never make. The rows the reading never reached are now counted and named, with the split beside them, so the close says what was actually seen | Files: `phantadex/runner.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 17:46:19 +0530]** | Stopped the closing split re-reading the ledger for rows the ledger cannot settle. `owner` consults the archive only for the self-submitted labels -- a discussion the user still has to post to -- but the check ran for every unfinished row, and the tree counts each row twice, once for its module and once for the course. On a course of eighty-odd rows that is a hundred and seventy full parses of a file that runs to a third of a megabyte, to answer a question whose answer could not change the verdict. The ledger is now read only where it decides something. No cache, so nothing new can go stale | Files: `phantadex/progress.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 17:46:19 +0530]** | Named a command's own option instead of blaming its value. The dispatcher stepped over any leading token starting with a dash, so `pdex --items 5 watch` walked past the unknown option, landed on the `5`, and reported the unknown command `'5'` -- true, and no help at all in finding the actual mistake. Options are now resolved against the global parser the way argparse itself resolves them, `--flag=value` and unambiguous prefixes included; an option the parser does not carry stops the scan and is reported as an option, with a line saying a command's flags go after its name | Files: `phantadex/cli.py`, `phantadex/config.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 17:46:19 +0530]** | Stopped a video without a transcript spending the warning meant for one whose markup had drifted. The transcript extractor reported a stale selector whenever both the panel scrape and the download fallback came back empty, and not every video carries a transcript -- one that does not renders neither the panel nor the toggle that opens it. Because the stale report deliberately speaks once per element per run, that first ordinary silence took the warning, and the next video whose selectors really had broken said nothing. Absence of the container is now told apart from failure to read it | Files: `phantadex/page_ops.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 16:19:56 +0530]** | Stopped a flag typed before the command from opening a page named after the command. A command was recognised only at the first position, and anything starting with a dash fell through to the default command -- whose optional URL then swallowed the next word. So `pdex -v watch` did not run a verbose watch: it ran the tree, took "watch" as the item to open, and navigated the user's own signed-in course tab to `/watch`, losing their place. `--log-level DEBUG archive` did the same with "archive". This is the case `urls.is_platform_url` exists to refuse and that branch never consulted it. The command is now found at the first token that is not a leading flag, stepping over the value a flag consumes -- so `--transcript-dir watch` still names a directory -- and a word that is neither a command nor a course link is refused rather than opened. The flags that take a value are read off the parser rather than listed by hand, so one added later cannot move the boundary. Live: `pdex -v dex` prints the tree instead of navigating to `/dex` | Files: `phantadex/cli.py`, `phantadex/config.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 16:19:56 +0530]** | Stopped the ledger writer from producing a file the ledger reader will quarantine. ElementTree passes characters XML 1.0 cannot represent -- a vertical tab, a form feed, a lone surrogate -- straight through to the file; the next read raises ParseError, and an unreadable ledger is renamed aside and rebuilt as an empty skeleton. The whole archived record of a course, lost to one control character. Scraped body text was already safe by accident, because the reading cleaner splits on those two characters, but a title comes off the sidebar untouched and a failure reason is whatever an exception happened to carry. Every tree is now scrubbed at the single write boundary rather than at each of the several places that set a title, a reason or a body: an invariant every caller must remember is one a caller eventually forgets, and here the cost of forgetting is the ledger | Files: `phantadex/course_manager.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 16:19:56 +0530]** | Recorded a failure for every item type, not only the archivable ones. `mark_failed` matched an existing ledger entry, and entries exist only for videos, readings and discussions -- so a lab, a plugin or a dialogue that failed three times matched nothing, logged one debug line and returned False. The promise that giving up is distinguishable from success held for exactly the types that already leave a file behind. The entry is now created when it is missing, the way the archiver already adopts an item the map did not list, so the failure is kept whatever the type; the shared entry-creation is one function instead of two copies. Archiving the item later still clears the failure | Files: `phantadex/course_manager.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 16:19:56 +0530]** | Stopped the run walking backwards off the end of a course. A locked item shows a Previous control and no Next; so does the last item of a course, and the detector could not tell them apart. Confirmed on the live course -- the final screen renders no Next at all. The run therefore treated the end as a lock, retreated, was skipped forward again by the sidebar, arrived nowhere new, and repeated until the stall guard ended a run that had in fact finished. The map settles it: an item it knows and places last is the end. An item the map does not know is never called last, because a slow lazy-load produces the same silence -- which is what `is_mapped` was written for | Files: `phantadex/runner.py`, `tests/test_runner.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 16:19:56 +0530]** | Gave `pdex discover` a bound and stopped it calling an empty read a success. Two opposite failures in the same poll: the Next-button fallback reports the click, not the navigation, so a Next that is swallowed -- or absent, this being the last item -- read as a successful hop that went nowhere, and the loop then waited for a URL change that would never come, in silence, forever. That is the shape the attempted-types trap closed on the sidebar path; the fallback branch needed its own. Meanwhile the exit condition was assigned from every sidebar read including an empty one, so a single unrendered outline emptied the list of types still to verify and closed the pass after one page, reporting the objective achieved. The pass now says so and closes out when nothing has moved for about a minute, and an empty read keeps the targets already known | Files: `phantadex/discovery/observation.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 16:19:56 +0530]** | Made discovery classify a survey the way a run will. The scanner read the URL segment before the title; the classifier deliberately reads a survey title first, because a survey is served as a supplement or a widget and the segment names its container rather than what the page asks for. So every survey was scanned as a reading and probed with the wrong selector categories -- the one answer a run arriving on that same page will never give. The scanner now mirrors the classifier's order, as the sidebar-row rule beside it already did | Files: `phantadex/discovery/rules.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 15:10:26 +0530]** | Made `pdex dex` read the ledger without creating one. Giving the tree a `CourseManager` so it could tell an archived item from an unarchived one handed it four side effects that belong to a run: the constructor makes the archive directory, renames a legacy ledger, takes the lock a live run holds, and writes a skeleton. So a tree printed for a course never run left an archive folder and an empty `.pdex.xml` behind, and a tree printed while a run was working blocked on the lock for thirty seconds. A read-only manager derives the same paths and answers the same questions, and returns before any of it. Checked against an empty directory: the tree printed and the directory stayed empty | Files: `phantadex/course_manager.py`, `phantadex/cli.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 15:10:26 +0530]** | Made `--pause-on-graded` wait on the half of a peer assignment it was walking past. A peer assignment is two sidebar rows, the submission and the review of classmates' work, and only the submission was named as graded -- so the flag returned one of them to the run's work and left the other on the skip list. A course whose last outstanding row was the review reported "nothing left to watch" and stopped, and the tip naming the flag was suppressed too, being gated on the same set. Both halves are answered by the user and neither is archived, so both are graded work | Files: `phantadex/progress.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 15:10:26 +0530]** | Stopped a reading's links carrying a body that never rendered past the length check. Appending the addresses before the minimum-length test was deliberate -- a page that is really a download button is a handful of words plus a URL, and the URL is the content -- but it also meant a body element that resolved and rendered nothing came back as a links block, comfortably over the twenty-character floor. That is the exact case the check exists to catch, and the reading handler's own comment says so: a body that will not render is a page to fail on, not one to dwell ten minutes on and mark complete. An empty body is now refused before the links are appended; a download button still renders its label, so the case the links exist for is untouched | Files: `phantadex/page_ops.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 15:10:26 +0530]** | Made the item budget spend the unit it quoted. `--items N` is checked against `CourseManager.item_count`, which deduplicates by item id on purpose -- ten items means ten things to sit through, not ten sidebar rows -- but the budget itself was keyed on the URL path. A peer assignment is one item id under two URLs, so it spent two of the bound the user had been quoted as one, and `--items 10` could end after nine real items. The same pass fixed `--modules N` refusing an item the map does not name: an unmapped row reports no module, which was excluded from the tally but not from the refusal, so a lazily-added row could end a run that still had a module to go | Files: `phantadex/runner.py`, `phantadex/limits.py`, `tests/test_runner.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-24 14:50:47 +0530]** | Coloured the unfinished marker bright red instead of dimming it. On a course tree that is mostly ticks, the handful of rows still outstanding are the reason the tree is being read, and they were the one thing on the page told to recede. Errors keep plain red, the darker of the two, so the pair stay distinguishable. Nothing measures the marker: the only width-sensitive line is the module header's right-aligned tag, which is padded by visible width, and the run log strips colour on the way in | Files: `phantadex/logs.py`, `CHANGELOG.md`
- **[2026-08-24 14:40:04 +0530]** | Split every count between the work Phantadex has left and the work you have left. "62 complete · 24 remaining" is the number a person actually reads, and it hid the only thing worth knowing: whether those 24 are a run away or an afternoon of quizzes. The rules that decide this all existed already -- the traversal's skip list, and the ledger rule that a discussion it holds is now waiting on your post -- but they lived in two modules and neither of the two places that print a count could see both. They are gathered into one module that owns the question, and the course tree and the run's closing report both read it: `89 items · 25 complete · 35 left to Phantadex · 29 left to you`, with a shortened form on each module header. Phantadex's own share is named even when it is zero -- "nothing left to Phantadex" -- because a line that simply stops after the completed count is the same line a course with work left prints, and telling those two apart is the whole reason to read it. Rows the sidebar cannot be read for are declared rather than dropped, so the parts add up to the total. `pdex dex` now opens the ledger to make the same distinction a run would, and still writes nothing to it. The split is by item type, not by settings: `--pause-on-graded` changes when a run reaches a graded item, never who answers it. That flag's line is also no longer phrased as though it described the run that just ended -- it is a tip, with a glyph of its own, saying what the flag does | Files: `phantadex/progress.py`, `phantadex/runner.py`, `phantadex/course_manager.py`, `phantadex/discovery/course_map.py`, `phantadex/cli.py`, `phantadex/logs.py`, `phantadex/overview.py`, `tests/test_progress.py`, `tests/test_runner.py`, `tests/test_discovery.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-24 14:28:01 +0530]** | Let the ledger settle a discussion the sidebar will never mark done. The platform counts a discussion prompt complete only once *you* post to it, so the row stays unfinished for the life of the course -- and on a course with everything else finished it was therefore the first unfinished item a run would act on, every run. Each one opened it, found the ledger already held its text, saved nothing, and then, having no other work to point at, handed the move to the platform's Next button, which walks to the adjacent row whether or not it is done: a finished reading, dwelled on and left. Measured on the live course, that was the whole of what a run did. A discussion the ledger already holds is now not offered as work at all -- the run reaches its verdict without opening anything -- and rather than report 'nothing left' beside a sidebar still showing the row, it names the item and says it waits on a post of the user's. The rule is the ledger's, not the map's, so a discussion not yet archived is still work. Live: the next-work scan went from naming the discussion to naming nothing, and the one row it now passes over is reported by title | Files: `phantadex/course_manager.py`, `phantadex/runner.py`, `tests/test_runner.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-23 21:30:58 +0530]** | Played the element whose state the run was reading. Picking the media element by longest duration fixed the reading whose two narration tracks confused the length, but only `state()` learned the rule: mute, play and seek still took `document.querySelector('video')`, the first node in document order. On a page with two players that drives one element while polling another -- `currentTime` never moves, the frozen-tick limit trips, and the item is abandoned as stalled, having also been seeked to a fraction of the other element's length. The selection is now one shared function every script calls, and each script names the element kind it acts on rather than assuming `video` | Files: `phantadex/video.py`, `tests/test_cli.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-23 21:30:58 +0530]** | Stopped the off-course recovery steering the tab back to the page it was escaping. The stuck check runs first on every tick and moves `last_url` to whatever is open, so by the time the recovery asked where the run had been, the answer was the sign-in page it had just been redirected to: it reloaded that, called the arrival a return to the course, and repeated until it gave up three passes later. The last URL that was actually inside a course is now kept separately, which is the only one worth returning to | Files: `phantadex/runner.py`, `tests/test_runner.py`, `CHANGELOG.md`
- **[2026-08-23 21:30:58 +0530]** | Kept the 'this reading did not render' guard for readings the archive already holds. Skipping the re-scrape also skipped the check, so a previously-archived reading whose body failed to load raised nothing, dwelled the listed seven to twelve minutes on an empty page and marked it complete. The body is read whichever way the ledger answers -- for a reading that is one locator lookup -- and what being archived saves is the save | Files: `phantadex/handlers.py`, `tests/test_handlers.py`, `CHANGELOG.md`
- **[2026-08-23 21:30:58 +0530]** | Paced a narrated reading by its narration even when its body cannot be found. The no-content path returned the listed duration untouched, so a reading that lost its body element to a slow load went back to the ten-minute stare the narration timing exists to remove. Without a body there is no word count and the listed duration is still the only estimate; with narration there is a measured length, and it wins | Files: `phantadex/interaction.py`, `CHANGELOG.md`
- **[2026-08-23 21:30:58 +0530]** | Sorted run logs by age rather than by the command that wrote them. Pruning kept the newest twenty by reverse name order, and a run log is named `{command}-{stamp}.log` -- so the command sorted first and the timestamp second. With the directory full of `watch` logs, a run named `archive` sorted below all of them and deleted the file it had just opened, after which it wrote the whole run into an unlinked inode. Latent, because only `watch` opens one today | Files: `phantadex/logs.py`, `CHANGELOG.md`
- **[2026-08-23 21:12:40 +0530]** | Kept the addresses a reading points at. `inner_text` renders an anchor as its label and drops its `href`, so a reading whose body *is* a link archived as words pointing nowhere: "Course Diagram" came out as the twenty-four characters `Course-Diagram PPTX File` with the slide deck's URL thrown away, and the four "Optional Resources for ... Case Study" pages kept the site names and article titles while losing every link. The body's anchors are now read alongside its text and listed under it -- fragment and script links skipped as interface, an address already written out in the prose not repeated, and the same address twice listed once. Nothing is downloaded; the file is named, not fetched. A body too short to keep on its own is kept when it carries a link, which is the attachment case, and a link walk that fails still returns the prose | Files: `phantadex/text.py`, `phantadex/page_ops.py`, `tests/test_text.py`, `tests/test_page_ops.py`, `tests/fakes.py`, `CHANGELOG.md`
- **[2026-08-23 19:41:18 +0530]** | Timed a narrated reading by its narration instead of by a guess about it. Coursera serves many readings with an audio track, and the dwell knew nothing of it: the wait was `min(listed, estimate * 2)`, so a 750-word glossary with 4:46 of narration was stared at for nine minutes, and a 1179-word syllabus narrated in 7:48 was held for the full ten the sidebar listed. One media reader now serves both element kinds -- the longest loaded `<audio>` or `<video>` on the page, because a reading was measured carrying two narration tracks with only one of them loaded, and taking the first in document order picked the empty one. With narration the dwell paces against the audio and leaves before it ends, which is also before the platform's own hand-off starts; without it the word-count estimate gets a settling constant and the listed duration caps it, and the doubling is gone. A page carrying no player at all is answered without waiting, so the plain readings pay nothing for the narrated ones. Measured on the live course: 10.0m -> 7.0m, 9.0m -> 4.3m, 10.0m -> 8.0m, and a three-word diagram page unchanged at the floor | Files: `phantadex/video.py`, `phantadex/timing.py`, `phantadex/interaction.py`, `phantadex/handlers.py`, `tests/test_video.py`, `tests/test_timing.py`, `tests/test_interaction.py`, `tests/test_handlers.py`, `CHANGELOG.md`
- **[2026-08-23 19:41:18 +0530]** | Let the platform and the user move the tab without the run pulling it back. `navigation.advance` has always deferred to a page that already moved; the work-based route added beside it did not, and it asked where to go *from the item the handler started on*. Coursera advances to the next item itself about five seconds after one completes, and a person may click any chapter at any moment -- either way the run navigated off whatever was now open and back to its own idea of next. Both routes now check first, on the principle that whoever moved the tab is right about where it should be, and a URL that cannot be read defers rather than guessing | Files: `phantadex/handlers.py`, `tests/test_handlers.py`, `CHANGELOG.md`
- **[2026-08-23 19:41:18 +0530]** | Stopped a run bouncing between an item it cannot finish and the one after it. A discussion prompt is read and archived but never posted to, so the sidebar lists it as unfinished for the whole run: leaving it, the work scan had no other answer and handed the move to the Next button, which landed on a completed item, from which the scan pointed straight back at the discussion. Observed live, repeating every eighteen seconds until the stall guard would have ended it forty passes later. An item the run has worked and left still listed is now remembered and not offered again, so the run reaches the end of what it can do and says so -- including how many of the items left are ones it worked on and cannot mark complete, because that needs a submission of the user's own | Files: `phantadex/runner.py`, `phantadex/course_manager.py`, `tests/test_runner.py`, `CHANGELOG.md`
- **[2026-08-23 19:41:18 +0530]** | Made a discovery pass reach the item types it had never been able to reach, and say so when it finds nothing. `DIALOGUE` and `DISCUSSION` were absent from the list of types a pass must cover, so it declared the course done before opening either -- which is why the `dialogue` selector category had never been probed on any course, on any machine. The transcript panel starts collapsed, and a collapsed panel leaves its toggle on screen and its body off it, so the probe verified the button and recorded it as the transcript; the panel is now opened first, through the same helper the scrape uses. An element no shipped selector matches is named rather than folded into "selectors are stable; no changes needed", which is the opposite report. And a run that meets markup it has no selector for now says once that the page has changed and names `pdex discover`, instead of degrading quietly item after item | Files: `phantadex/discovery/rules.py`, `phantadex/discovery/probing.py`, `phantadex/page_ops.py`, `phantadex/schema.py`, `tests/test_discovery.py`, `tests/test_schema.py`, `CHANGELOG.md`
- **[2026-08-23 16:03:24 +0530]** | Moved a run between the items it has work on rather than between adjacent rows. Resuming already jumped to the first unfinished item, but from there the traversal walked: it advanced one row at a time, opened every finished item on the way, and paid a completion prompt on each to learn what the sidebar could have told it before it left. A handler now asks the runner where the next work is -- the sidebar is read at the item boundary, the items ahead are preferred to the items behind, and the item just finished is never the answer, because the sidebar takes a moment to record a completion. The platform's Next button remains the fallback for when that scan cannot answer, and the one case that looks like an ended course but is not -- the only work still listed being the item just left -- falls back to it rather than declaring the run over. Confirmed on the live course: the run left a discussion prompt and opened the next reading directly, with no finished item opened in between | Files: `phantadex/runner.py`, `phantadex/handlers.py`, `phantadex/course_manager.py`, `phantadex/overview.py`, `tests/test_runner.py`, `tests/test_handlers.py`, `tests/test_course_manager.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-23 16:03:24 +0530]** | Made the course map and the open page agree about what a survey is. `apply_filler_override` gated on one list of keywords and then required a second, narrower one to actually fire, so "Welcome! Please Tell Us About Yourself" -- a phrase in the first list and in the classifier's own -- could never become `FILLER`. The map called it an ungraded plugin, a run picked it as the first thing left to do, navigated to it, and only on arrival recognised a survey and stepped past it. The rule now reads `detection.SURVEY_TITLES` directly, so a row the map calls filler is exactly the row a run will call a survey. The phrases stay whole ones: a reading titled "A Survey of Reinforcement Learning" is course content and is still archived | Files: `phantadex/discovery/rules.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-23 16:03:24 +0530]** | Stopped re-scraping an item the archive already holds. The runner reported "already archived in ledger" and then extracted and saved the same text anyway -- a transcript costs a tab click and a scrape, and the file was rewritten with the same words. Only the pre-skip archive checked; the video, reading and discussion handlers now check too, and skip the extraction while still working the item, because being archived says nothing about whether the platform counts it as done. `pdex archive --force` remains the way to deliberately scrape again | Files: `phantadex/handlers.py`, `tests/test_handlers.py`, `CHANGELOG.md`
- **[2026-08-23 16:03:24 +0530]** | Counted the archiver's finished work against the work there is. The ledger holds every item a run archived, discussion prompts included, and `pdex archive` visits only videos and readings -- so on a course with one archived discussion the line read "62 archivable items (63 already done)", more done than there were to do. The count is now the intersection. The same run is also laid out the way the course is: items are grouped under their module with a count on the header, and every line names the item's type | Files: `phantadex/archive.py`, `tests/test_archive.py`, `CHANGELOG.md`
- **[2026-08-23 16:03:24 +0530]** | Gave item types one spelling and one colour wherever they are printed. The course tree lowercased the map's label, the traversal wrote its own words for the same kinds, and the archiver named no type at all; `FILLER` and `REVIEW_PEERS` reached the screen as themselves, which says nothing to the person reading the line. One table in `logs` now carries the reading name and the colour -- what is played, what is archived, what is left to you, and what is walked past -- and the tree, the handlers and the archiver all read it. The right-aligned tags stay uncoloured on purpose: an escape sequence has length but occupies no columns, and the padding is measured from it | Files: `phantadex/logs.py`, `phantadex/runner.py`, `phantadex/handlers.py`, `phantadex/archive.py`, `phantadex/discovery/course_map.py`, `tests/test_logs.py`, `CHANGELOG.md`
- **[2026-08-23 15:20:13 +0530]** | Ended a run that has nothing left to do instead of touring the course to prove it. The resume scan targeted the first item the sidebar marked unfinished whatever kind it was, so a course whose remaining items were all graded resumed on a peer assignment, named it, stepped past it, and then walked every finished item after it with a thirty-second skip prompt on each. Graded work is left to the user and nothing is archived from it, and the same is true of the surveys the map labels `FILLER`, so an item carrying one of those labels is not work a run still has: resume now targets the first unfinished item the run would actually act on, and when there is none it says what is left and by what kind, and stops. `--pause-on-graded` puts the graded items back among the work, because then the run does wait on them; a survey stays skipped either way. Checked against the live course this was reported on: 26 of its 86 rows are unfinished and only 4 of them are work | Files: `phantadex/runner.py`, `phantadex/course_manager.py`, `phantadex/overview.py`, `tests/test_runner.py`, `tests/test_course_manager.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-23 13:50:29 +0530]** | Kept one run to one run log, and kept the test suite out of the state directory a person's own runs write to. The package logger outlives a single run inside a long-lived process, and the file handler was added without the previous one ever being removed -- so a second run wrote every record to both files, a third to all three, and the suite, which starts the watch entry point once per flag it checks, left the user's log folder holding hundreds of kilobytes of test output with each line repeated once per test. The handler now carries a type of its own so it can be found again, and opening a run log closes the one already installed; importing the test package points `PHANTADEX_STATE_DIR` at a disposable directory, which covers the resume ledger and the learned selectors as well as the logs | Files: `phantadex/logs.py`, `tests/__init__.py`, `tests/test_logs.py`, `tests/test_cli.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-23 13:41:00 +0530]** | Bounded how far one run goes, with `--modules N` and `--items N`, and bumped the package to 2.2.0. N is validated at the command line -- below one is refused rather than read as "no limit", and the two flags are mutually exclusive because they bound the same run in two ways and honouring the smaller one silently is worse than saying they conflict. The count runs from where the run starts, not from the first item of the course: a run resumes at the first unfinished item, so "the next two modules" is the only reading under which the flag does something useful on a course half done. The bound is read on arrival at an item and before it is handled, so a run that stops has stopped without touching the item it refused, and it says what it covered. A limit larger than the course is reported against the loaded map rather than refused | Files: `phantadex/limits.py`, `phantadex/config.py`, `phantadex/runner.py`, `phantadex/course_manager.py`, `phantadex/overview.py`, `phantadex/__init__.py`, `tests/test_limits.py`, `tests/test_runner.py`, `tests/test_cli.py`, `tests/test_course_manager.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-23 13:41:00 +0530]** | Stopped a run cycling on the last item of a course, and gave every run a bound it cannot spin past. A peer assignment is two sidebar rows -- the submission and the review of classmates' work -- served under one opaque item id, so `same_item` treats them as one item, correctly and by design. `get_next_url` matched the first of the two and returned the row after it, which is the page already open: `goto` was a no-op, the handler advanced again, and the pair repeated until the run was killed. Every row belonging to the current item is now stepped over, in both directions, leaving `same_item`'s identity semantics untouched because move detection, resume matching and the archive check all rest on them. Two layers sit behind that: the ledger fallback refuses a destination that is the item already open, on the grounds that reaching there means the map repeats itself; and a stall guard ends any run that spends forty passes without arriving at an item it has not been on, which is the general bound the traversal never had -- its ways of spinning were each individually recoverable and none of them was individually counted. An unclassifiable page that also cannot be advanced past now waits between attempts instead of repeating warn-and-fail as fast as the loop could turn | Files: `phantadex/course_manager.py`, `phantadex/navigation.py`, `phantadex/limits.py`, `phantadex/runner.py`, `phantadex/config.py`, `tests/test_course_manager.py`, `tests/test_navigation.py`, `tests/test_limits.py`, `tests/test_runner.py`, `CHANGELOG.md`
- **[2026-08-23 13:41:00 +0530]** | Kept a log record from landing on the line a progress bar is repainting. The bar and the spinner write in place with a carriage return and no newline, so any record arriving mid-animation was welded onto the bar's last frame -- "poll skipped" appended to a half-drawn bar, which then stayed frozen in the scrollback because the next repaint started after the message. Modal dismissals made it visible, being the most frequent, but the collision was systemic and every warning and `-v` debug line had it. The in-place line is now remembered as well as written, and the console handler blanks it, emits the record on a line of its own and repaints the animation where it was, all under the lock the writer holds -- so the animation survives the interruption rather than being cut off by it | Files: `phantadex/logs.py`, `phantadex/overview.py`, `tests/test_logs.py`, `CHANGELOG.md`
- **[2026-08-23 13:41:00 +0530]** | Kept a full record of every run on disk. Nothing was saved: a run that went wrong left only whatever the terminal still held, and a quiet run left less than that. Each run now writes one timestamped file to a `logs/` directory beside the learned selectors, at DEBUG whatever the console was asked to show -- the package logger carries everything and the console handler carries the level, so the two are independent. Colour is stripped on the way in, the twenty most recent files are kept, a directory that cannot be opened is reported rather than fatal, and `--no-run-log` writes none | Files: `phantadex/logs.py`, `phantadex/config.py`, `phantadex/watch.py`, `phantadex/overview.py`, `tests/test_logs.py`, `tests/test_cli.py`, `CHANGELOG.md`
- **[2026-08-23 13:41:00 +0530]** | Handled the tab being on the platform but not inside a course. The home page, the catalogue and the enrolment list are the same host and match nothing a course item matches, so a run that landed on one had no map, no handler and nowhere to advance to -- it classified the page as `UNKNOWN` and spun on it. A tab already inside a course is now preferred when a command picks among several, a run that starts on such a page says so and names the way out instead of attaching to it, and a run that drifts onto one mid-session steers back to the item it was last on, giving up after three attempts rather than waiting on a tab that will not return. The warning shown when several tabs are open no longer calls them course tabs when none of them is inside a course, which contradicted the line printed directly after it | Files: `phantadex/urls.py`, `phantadex/session.py`, `phantadex/runner.py`, `tests/test_urls.py`, `tests/test_session.py`, `tests/test_runner.py`, `CHANGELOG.md`
- **[2026-08-23 00:29:42 +0530]** | Signed every ledger and bumped the package to 2.1.0. A `.pdex.xml` now opens with a generator comment in its prolog naming Phantadex, the version that wrote it and the repository -- above the root element, where an XML reader expects a generator note and where nothing walking the tree will meet it as data. `ElementTree` holds one root and drops comments when it parses, so the banner is spliced into the serialized bytes and rebuilt on every write; appending it instead would have stacked one banner per save. It names the tool and nothing about the machine, the account or the run, and tests hold it to that as well as to being a well-formed comment. The version lives in `phantadex/__init__.py` alone, which `pyproject.toml` already reads, so the bump is one line and a ledger records which release produced it | Files: `phantadex/course_manager.py`, `phantadex/__init__.py`, `tests/test_course_manager.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-23 00:25:32 +0530]** | Coloured the help page the way a run colours its output, and tracked the document it points at. `-h` was the one surface printing flat text while every command around it painted its subject: headings are now bold, command and option names and the archive paths take the cyan a run gives its subjects, item types the green, the rules dim, and the *NEVER DOES* bullets red. Measurement is the part that had to change with it -- an escape sequence has a length but occupies no columns, so rules are sized and the two-column layout padded by visible width, and tests now render the page twice and require the coloured and plain forms to differ by nothing but colour. Colour still yields to `NO_COLOR` and to a redirected stream, and a Windows console has virtual-terminal processing turned on first so the codes render rather than print literally, with the `kernel32` boundary declared because a console handle is pointer-wide. `docs/USAGE.md` is tracked at last, and the *NEVER DOES* section links it at its address in the repository rather than naming a path a reader installing from PyPI does not have | Files: `phantadex/overview.py`, `phantadex/logs.py`, `docs/USAGE.md`, `tests/test_cli.py`, `CHANGELOG.md`
- **[2026-08-22 17:43:47 +0530]** | Pointed two video controls at the elements they name. `current_time` listed `span[aria-label='Video Progress']` and `div[role='slider']` ahead of nothing else, and `duration` fell back to `div.video-player-progress-bar span:last-child`; checked against a live player, all three resolve to the progress bar's drag handle, whose text is empty -- so a timestamp read through either category returned an empty string rather than a time, and the same handle was mapped to three categories at once. Each now lists only its own element with a tagless fallback, and a test fails if any two categories claim one selector or if a slider is named as a timestamp | Files: `phantadex/element_schema.py`, `phantadex/video.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-22 16:49:54 +0530]** | Left no named trace of the media guard in the documents it arms. The guard published `window.__phantadexMediaGuard`, findable by any page script with one property lookup, and wrapped `HTMLMediaElement.prototype.play`, whose replaced `toString` and source readable through `Function.prototype.toString` were both irregularities a page script could find. State now lives behind a symbol registered under a fixed token: absent from `Object.keys` and `Object.getOwnPropertyNames`, though a registered symbol is still reachable through `Object.getOwnPropertySymbols` and `Symbol.keyFor`, so this is one named property fewer rather than concealment. A per-process random token was tried and reverted -- it bought nothing against that enumeration and cost recoverability, because only a run holding the same key can disarm a guard, and a killed run's tab kept re-muting whatever the user unmuted. Listeners are removed through an AbortController instead of stored handles, and the prototype is left exactly as the browser shipped it; the capture-phase mutes at `loadstart` cover what the wrap did. The host check is also exact rather than a bare suffix, which used to arm inside lookalikes such as `notcoursera.org` | Files: `phantadex/interaction.py`, `tests/test_interaction.py`, `tests/test_branding.py`, `CHANGELOG.md`
- **[2026-08-22 16:49:54 +0530]** | Sought through the player's own timeline control before reaching for a direct `currentTime` write. A seek that assigns `currentTime` fires none of the pointer events dragging the timeline would, so the player's record shows a jump with no interaction behind it. The run now reveals the auto-hiding control bar, clicks the progress bar at the target fraction through the shared click path with position jitter, and confirms playback arrived near the point clicked before trusting it -- "the position moved" proves nothing, since playback is already running and advances a second per second whatever the click did; the direct script remains as the fallback when no bar matches or the click does not land. The player is also paused through its control once a video meets its completion target, so a run no longer advances leaving playback running behind it. New `video_controls.progress_bar` and `pause_button` selector candidates back both paths, the former corrected against the live player: the timeline is a span, so the `div.` form matched nothing, and the two selectors carrying the slider role matched the 10px drag handle riding on it. A candidate narrower than half the player is now rejected before it is clicked | Files: `phantadex/video.py`, `phantadex/handlers.py`, `phantadex/element_schema.py`, `phantadex/interaction.py`, `tests/test_review_regressions.py`, `tests/test_handlers.py`, `tests/test_interaction.py`, `CHANGELOG.md`
- **[2026-08-22 16:49:54 +0530]** | Gave AI roleplay dialogues a session shape a person could have had. The handler opened a dialogue, waited four to eight seconds, sent nothing and closed it -- a backend record of turns: 0 over seconds that no conversation produces. The item is now held open for a drawn 45-120 seconds in slices with occasional cursor drift near the middle of the window; composing turns remains out of scope by design. Its clicks also travel the shared interaction path at last: the raw locator click teleported the cursor to the element centre with no approach, no reaction dwell and no bounded wait, so one stuck control could sit out Playwright's 30-second default ten times over | Files: `phantadex/handlers.py`, `tests/test_handlers.py`, `CHANGELOG.md`
- **[2026-08-22 16:49:54 +0530]** | Drew every remaining fixed wait from the jitter module. Item transitions settled for exactly 5.0s four ways per advance, detection and handler failures idled exactly 2s, resume and reload settles were flat, transcript toggles waited 1.5s, modal dismissals 1.0s, and the video watch ticked at precisely 1s -- hundreds of items deep, each was a deterministic beat in the record. Each now draws from a range; the config-driven idle poll and local URL polls stay as they are, having nothing to do with what the platform can observe. Modal dismissal also tries its click with actionability checks on before forcing through an animating overlay, so a mid-animation dismiss can no longer land on whatever sits behind it | Files: `phantadex/navigation.py`, `phantadex/runner.py`, `phantadex/page_ops.py`, `phantadex/modals.py`, `phantadex/handlers.py`, `tests/test_jitter.py`, `tests/test_interaction.py`, `CHANGELOG.md`
- **[2026-08-22 16:49:54 +0530]** | Said what the browser relationship actually is, everywhere. The overview and three README passages claimed Phantadex attaches to "the Chrome you already use" and never launches its own, while the launcher section correctly described a dedicated debug profile -- the two could not both be true. The claims now match the launcher, and the README notes plainly that the debugging port is open on localhost for as long as the debug window lives. `docs/USAGE.md` is written up properly as the documented way to clear the graded work runs step past, working from the `.pdex.xml` materials a run has archived, and the help page's *NEVER DOES* section points at it | Files: `README.md`, `phantadex/overview.py`, `docs/USAGE.md`, `HANDOVER.md`, `CHANGELOG.md`

- **[2026-08-22 10:22:03 +0530]** | Documented installation from PyPI now that `phantadex` 2.0.0 is released. The install section described the index route as hypothetical and the clone as the current method; the two have swapped, with upgrade commands and the note that learned selectors survive an upgrade because they are stored outside the package. The version badge reads from PyPI instead of being hardcoded, so it cannot fall behind a release | Files: `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-22 08:49:59 +0530]** | Trimmed the two prose documents to what they are each for. `HANDOVER.md` restated changes already recorded here and listed verification evidence item by item; it now carries only the constraints that still bind the code -- the traps, the design decisions in force, and what has and has not been exercised against a live session. `README.md` described each item handler in both the overview and the limitations section, which is the duplication that let the overview go stale; the per-type behaviour now has one home in section 8, and the overview points at the flags, that section and `pdex -h` | Files: `HANDOVER.md`, `README.md`, `CHANGELOG.md`
- **[2026-08-22 08:35:38 +0530]** | Narrowed what `pdex stop` recognises as a run. Any token that looked like a path was matched against the entry-point names whatever program held it, so `vim phantadex_watch.py`, `git commit phantadex_watch.py` or `tail -f phantadex_archive.py` were terminated along with the runs -- taking the user's editor with them. A file named on the command line now counts only when the executable is a Python interpreter; the executable itself still counts on its own. Verified live: a real `pdex watch` is still found and stopped, and the decoys are not | Files: `phantadex/processes.py`, `tests/test_processes.py`, `CHANGELOG.md`
- **[2026-08-22 08:35:38 +0530]** | Declared the Win32 boundary before probing whether a process is alive. `ctypes` assumes an undeclared function returns a 32-bit signed integer, but a HANDLE is pointer-wide, so a 64-bit handle with its high word set came back negative and sign-extended on the way into `GetExitCodeProcess`, which rejected it. A process that had already exited then read as alive and `pdex stop` waited out the grace period for nothing | Files: `phantadex/processes.py`, `CHANGELOG.md`
- **[2026-08-22 08:35:38 +0530]** | Discarded a downloaded transcript once it has been read. Playwright clears a download when its browser context closes, and this context is the user's own Chrome attached to over CDP, which does not close -- so a bulk archive left one temporary file per transcript behind for as long as the browser lived. A failure to clean up no longer loses the transcript already in hand | Files: `phantadex/page_ops.py`, `tests/test_page_ops.py`, `CHANGELOG.md`
- **[2026-08-22 08:35:38 +0530]** | Corrected the attach-failure message. It pointed at `python3 scripts/start_chrome_debug.py`, a path an index install does not have since the launcher moved into the package, and named port 9222 regardless of the configured endpoint. It now names `pdex chrome`, reads the port from `--cdp-url`, and suggests `netstat` on Windows rather than `lsof` | Files: `phantadex/session.py`, `tests/test_session.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Stopped a discovery pass from promoting a last-resort selector over the one shipped above it. `_probe_element` returns the first selector that is *visible*, which is not the same as the best one: a closed transcript panel hides its container while leaving the "Transcript" toggle on screen, so the toggle was recorded as the transcript body and every later scrape read the player's control text (52 characters, nothing after cleaning) instead of the transcript. A probe result that ranks below the effective selector in the element's own candidate list is no longer recorded. Verified live: the toggle is no longer written, and transcript extraction on the same video returns 1,758 characters via `UI_Scrape` | Files: `phantadex/discovery/probing.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Handed the merged selector view back to the discovery loop instead of the learned state alone. `discover_selectors` seeds what it saves from the state file, so that writing it cannot bury the packaged defaults, but it also returned that narrow mapping as the session's selectors. From the second item onwards every shipped default read as absent: they were relabelled `NEW` and written into the state file, and the hop between items lost `navigation.next_item`, which no state file need ever have carried | Files: `phantadex/discovery/probing.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Stopped the discovery loop spinning on a sidebar target that never clears itself. A row's mapped type and the type its page reports need not agree, so a jump could leave the type still missing; the next hop then re-issued the same `goto`, the poll's URL-change check never fired again, and the pass sat at two-second intervals in silence. Each type is now jumped to once, and the closing message names any type left unverified rather than claiming the objective was met | Files: `phantadex/discovery/observation.py`, `phantadex/discovery/state.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Kept a discovery pass running when one item cannot be read. A tab caught mid-navigation or a frame detached under the probe propagated out of the poll and ended the whole session; the item is now reported, marked seen so the failure is not retried every two seconds, and the pass continues | Files: `phantadex/discovery/observation.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Narrowed the selector categories scanned on a page type nobody has characterised. `WRAPUP`, `SURVEY`, `FILLER` and `UNKNOWN` fell back to the entire schema, which is how a content selector comes to be "verified" against an unrelated element that happened to match -- and a verified selector outranks the shipped default until the next run. They now scan the categories every page has | Files: `phantadex/discovery/probing.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Stopped rewriting a selector entry that holds a list of candidates. Only the first entry of such a list is ever used, but the probe result was compared against the list object, which is never equal, so the entry was reported `MODIFIED` on every pass and rewritten as a bare string -- collapsing the entry and dropping the fallbacks kept behind it. The comparison now reads the effective value, and a genuine replacement keeps the remaining fallbacks | Files: `phantadex/discovery/probing.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Made `--pause-on-graded` actually wait on a graded quiz. The wait ended after two consecutive polls found no quiz markup, about four seconds, and a graded quiz sits behind a start screen where its markup is absent before the attempt as well as after one -- so the run announced a pause and advanced almost immediately. Absence now only counts once the quiz has been seen; otherwise the wait runs until the item is left, which is what the graded-assignment handler has always done. A line every minute reports that the wait is still open | Files: `phantadex/handlers.py`, `tests/test_handlers.py`, `tests/test_review_regressions.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Said so when a run has no ledger. If the course name could not be read or the map came back empty, `_sync_course_map` returned quietly and the run continued with no manager: nothing was archived, no navigation fallback worked, and the output was indistinguishable from a course with nothing to save. The reason is now reported once per failure rather than once per tick | Files: `phantadex/runner.py`, `tests/test_runner.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Reworked the `pdex -h` page: section rules sized to the widest line beneath them, a wrapped second column so no line runs past eighty columns, and a link to the repository. The page is ASCII throughout -- it prints before logging widens stdout to UTF-8, and a redirected stream on Windows inherits the ANSI code page, where a box-drawing rule would raise `UnicodeEncodeError` on the first line of help. The repository link is checked against the `Homepage` in `pyproject.toml` so the help page and the published metadata cannot drift | Files: `phantadex/overview.py`, `phantadex/__init__.py`, `tests/test_cli.py`, `CHANGELOG.md`
- **[2026-08-22 02:46:01 +0530]** | Ignored discovery state written into the working tree, and ledgers written outside the archive directory, so a run with `PHANTADEX_STATE_DIR` pointed at the checkout cannot commit its own scraped course data | Files: `.gitignore`, `CHANGELOG.md`
- **[2026-08-22 00:38:58 +0530]** | Stopped matching a sidebar row's type against its own title. `classify_sidebar_row` concatenated the row subtext with its `aria-label`, and the label carries the item title after the type, so a reading titled "Digital Media Consultant Activity for Module 4 Peer Assessment" matched "peer" and was mapped `PEER_REVIEW` -- named in the course map as work to submit, when it is a page to read. Each source is now matched on its own, and the label contributes only its first comma-separated field, which is where Coursera puts the type. Verified against the live sidebar: both readings named after the peer assessment now read as readings, while the assessment itself and its "Review Your Peers" step are unchanged | Files: `phantadex/discovery/rules.py`, `tests/test_discovery.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-22 00:38:58 +0530]** | Let every course command be pointed at an item: `pdex <url>`, and the same optional argument on `dex`, `skip`, `watch`, `archive` and `discover`, accepting a full link or a `/learn/...` path. It settles which course is meant, which a tab scan could not: `find_course_page` took the first platform tab Chrome listed, which is the order tabs were opened in, not the order they were last used. With no URL and several course tabs open the choice is now reported rather than made silently. A bare link as the first argument runs `dex` against it; anything else unrecognised is still reported as an unknown command | Files: `phantadex/session.py`, `phantadex/config.py`, `phantadex/cli.py`, `phantadex/watch.py`, `phantadex/archive.py`, `phantadex/runner.py`, `phantadex/urls.py`, `phantadex/overview.py`, `phantadex/discovery/observation.py`, `tests/test_session.py`, `tests/test_cli.py`, `tests/test_urls.py`, `tests/test_branding.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-22 00:20:48 +0530]** | Added `pdex -h`, a single page describing every command, what a run does with each kind of course item, the pacing defaults, where files are written, and what is never automated. Argparse's own top-level help listed command names with no room to say what any of them did. The page is derived rather than restated: the command list is the help page's own table, so a dispatchable command cannot go undocumented, the per-item lines come from each registered handler's `summary`, and the pacing and ledger figures are read from the modules that enforce them. `config.program_name` was added at the same time so every subcommand's usage line names the subcommand and can be copied and run as printed, instead of every one of them claiming to be the bare entry point | Files: `phantadex/overview.py`, `phantadex/cli.py`, `phantadex/config.py`, `phantadex/handlers.py`, `phantadex/archive.py`, `phantadex/chrome.py`, `phantadex/watch.py`, `tests/test_cli.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-22 00:20:48 +0530]** | Gave the ungraded widget and external-tool rows their own `UNGRADED_PLUGIN` map label instead of calling them `LAB`. Coursera's row subtext reads "Ungraded Plugin" for those and "Ungraded Lab" for a lab, so one label named neither of them correctly in the map. The `/ungradedLab/` segment keeps `LAB`; both labels still collapse onto the single plugin handler, so run behaviour is unchanged | Files: `phantadex/detection.py`, `phantadex/discovery/rules.py`, `phantadex/discovery/probing.py`, `tests/test_discovery.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 23:53:08 +0530]** | Gave Coursera's AI roleplay practice its own `DIALOGUE` type and handler instead of folding it in with the ungraded widgets. A widget only needs its box ticked; a dialogue has to be opened and then explicitly closed -- `Start Dialogue`, `End Dialogue`, `Yes, end the Dialogue` -- before Coursera marks the item complete. Verified on a live item: the sequence runs in about ten seconds and the sidebar row moves from "Not submitted" to "Completed". A dialogue an earlier run left open is closed from wherever it is rather than restarted, and one already ended is stepped past untouched. Nothing is archived: the content is a conversation the run does not hold up its end of. Ungraded plugins and LTI items keep the type and handler they had | Files: `phantadex/detection.py`, `phantadex/handlers.py`, `phantadex/element_schema.py`, `phantadex/runner.py`, `phantadex/discovery/rules.py`, `phantadex/discovery/probing.py`, `tests/test_handlers.py`, `tests/test_discovery.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 23:53:08 +0530]** | Bounded the content the ledger stores per item at 40,000 characters. A supplement can embed a PDF viewer whose rendered text is already extracted through the reading body, and a book-length one would be written into the ledger whole, making it slow to parse and unreadable. The `.txt` file beside it is the archive and is still written in full; the ledger now keeps an excerpt cut on the last line break inside the budget, followed by the character count and the name of the file holding the rest. The longest reading seen so far is under 20,000 characters, so ordinary content is unaffected | Files: `phantadex/course_manager.py`, `tests/test_course_manager.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 23:25:49 +0530]** | Recognised the `/coach/` segment -- Coursera's AI roleplay practice, whose row subtext reads "Dialogue" -- as an ungraded widget. It previously scanned as `UNKNOWN`, so every schema category was tried against it, and because the segment carried no item id two roleplay items compared as the same item. It is now handled like the other ungraded widgets: stepped past, not archived | Files: `phantadex/detection.py`, `phantadex/urls.py`, `phantadex/discovery/rules.py`, `tests/test_discovery.py`, `tests/test_urls.py`, `CHANGELOG.md`
- **[2026-08-21 23:16:44 +0530]** | Read a sidebar row's lines from its element tree instead of `innerText`, fixing titles that swallowed their own subtext. Coursera ships the outline minified, and `innerText` on a row that is not being rendered -- every row of a collapsed module -- yields nothing, so the old code fell through to `textContent`, which concatenates with no separator ("Module 4 OverviewReading. Duration: 20 minutes"). The title took the whole string and the subtext, which is what distinguishes a graded row from an ungraded one sharing a URL segment, came back empty. Measured against the live sidebar: 57 of 76 rows read blank under `innerText`, all 57 now parse into title and subtext, with no change to any row's type or duration. The walk also drops the per-row scroll-and-wait those rows used to need | Files: `phantadex/discovery/row_text.py`, `phantadex/discovery/course_map.py`, `phantadex/discovery/context.py`, `tests/test_discovery.py`, `tests/fakes.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 21:21:12 +0530]** | Skipped survey items outright: a title naming a questionnaire about the learner classifies as `SURVEY`, ahead of the URL segment, and its handler advances without dwelling, archiving or answering. The segment names the container a survey is served in (supplement or widget), not what it asks for. Each marker is a full phrase, so a reading titled "A Survey of Reinforcement Learning" is still archived as course content | Files: `phantadex/detection.py`, `phantadex/handlers.py`, `tests/test_detection.py`, `tests/test_handlers.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 21:21:12 +0530]** | Made the media guard reversible and gave its wrapper the identity of the method it replaces. `BrowserSession` releases the guard on exit -- listeners removed, `HTMLMediaElement.prototype.play` restored -- because the tab outlives the run and a guard left in place re-mutes whatever the user unmutes by hand until they reload. Verified live: under the guard `play.name` reads `play` and `String(play)` reads as native, and after the session leaves an unmute holds at `volume=0.5`. Media is left as it is on release; restoring the volume would put sound through a speaker nobody asked to be sitting at | Files: `phantadex/interaction.py`, `phantadex/session.py`, `tests/test_interaction.py`, `tests/test_branding.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 21:21:12 +0530]** | Matched modal controls by `aria-label` as well as visible text, so an icon-only close button is reachable, and added `Close` to the ways out of the demographics survey | Files: `phantadex/modals.py`, `tests/test_modals.py`, `CHANGELOG.md`
- **[2026-08-21 20:59:45 +0530]** | Stopped answering the demographics survey on the user's behalf: the rule offered `Continue` and `Submit`, either of which submits a survey about them while they are not present. It now looks only for declining controls (`Skip`, `No thanks`, `Not now`...), and a modal that no rule can clear is reported once as a warning instead of only in the debug log | Files: `phantadex/modals.py`, `tests/test_modals.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 20:42:17 +0530]** | Muted platform media before it can be heard rather than on the next loop tick: a guard installed on the browser context runs ahead of the page's own scripts and silences every media element at `loadstart`, re-silencing on any later attempt to raise the volume. Measured against a live lecture, an unguarded load reported `muted=false, volume=1` at `loadstart`, `loadedmetadata` and `canplay`; with the guard the same load reported `muted=true, volume=0` at all three, and a forced `muted=false; volume=1; play()` still produced silence at `play` and every `timeupdate`. The guard is scoped to the platform host, since the context is the user's own browser profile | Files: `phantadex/interaction.py`, `phantadex/session.py`, `tests/test_interaction.py`, `tests/test_branding.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 20:42:17 +0530]** | Recorded archived items the ledger did not list instead of reporting `ledger unmatched` and leaving the file unaccounted for. The sidebar row type and the live page classification can disagree -- a supplement titled as a survey maps to `FILLER` and never reaches the ledger, yet reads as a reading once open -- so an archived item is now filed under the module the map gives it, or a catch-all module when the URL is unknown, with the type the saved content implies | Files: `phantadex/course_manager.py`, `tests/test_course_manager.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 19:48:04 +0530]** | Replaced the uniform draws behind the observable delays -- click reaction, reading scroll pause, post-target dwell, and the two archive pacing gaps -- with a log-normal draw over the same span plus a 4% chance of a longer pause (`phantadex/jitter.py`). Uniform gaps have a hard floor, a hard ceiling and flat density between them, which no person produces; the replacement keeps the same mean, so run pace is unchanged. Percentages and pixel offsets stay uniform, where a skew would bias every click toward one edge of the control | Files: `phantadex/jitter.py`, `phantadex/interaction.py`, `phantadex/handlers.py`, `phantadex/archive.py`, `tests/test_jitter.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 19:32:40 +0530]** | Made the video completion target a range sampled once per video rather than one fixed percentage: `--video-threshold` now takes `MIN[-MAX]` and defaults to `98-100`, so two runs of the same video do not advance at the same instant. A single number is still accepted and is held as a range of zero width, including when `Settings` is constructed directly | Files: `phantadex/config.py`, `phantadex/handlers.py`, `tests/test_cli.py`, `tests/test_review_regressions.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 19:11:15 +0530]** | Probed process liveness through the Win32 handle on Windows: signal 0 is `CTRL_C_EVENT` there, not a no-op, so `os.kill(pid, 0)` delivered a console interrupt to the target's process group -- which on CI included the test process running the probe | Files: `phantadex/processes.py`, `tests/test_processes.py`, `CHANGELOG.md`
- **[2026-08-21 19:05:41 +0530]** | Added `pdex stop`, which ends every Phantadex process on the machine regardless of where it was started: each is asked to exit before it is killed, and the scan skips the stopping process and its own ancestors. Runs are found from the process table rather than a registry file, because a registry survives a hard kill and then reports processes that are gone; only the executable and the tokens that name a file are matched, so a command that merely mentions the entry point is never targeted | Files: `phantadex/processes.py`, `phantadex/cli.py`, `tests/test_processes.py`, `tests/test_cli.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 19:05:41 +0530]** | Turned the pre-watch seek off by default: `pdex watch` now plays a video through, with `--skip` requesting the old jump into `97.5-98.5%` and `--video-skip-range` implying it. The completion threshold default moved from 100% to 98%, which is where a video is credited without waiting on the final frames. `pdex skip` is unchanged -- seeking is what that command is for | Files: `phantadex/config.py`, `tests/test_cli.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 19:05:41 +0530]** | Attached the discovery loop through `BrowserSession` instead of its own `connect_over_cdp` block, so a failed attachment reports the same actionable diagnosis as every other command and Playwright is always stopped. The loop keeps its own tab lookup: `BrowserSession.find_course_page` raises the tab to the front, which at a two-second poll would take the user's focus every cycle | Files: `phantadex/discovery/observation.py`, `CHANGELOG.md`
- **[2026-08-21 18:20:17 +0530]** | Required a frame match to be on screen before discovery records it: the frame branch accepted any match on the grounds that visibility is awkward across frames, so a hidden element could be persisted as a *verified* selector, and verified selectors outrank the packaged defaults until the next discovery run | Files: `phantadex/discovery/probing.py`, `tests/test_discovery.py`, `CHANGELOG.md`
- **[2026-08-21 18:20:17 +0530]** | Matched URL path segments case-insensitively in `detection._segment`, which compared exact case while `urls.item_id` already lowered before its own lookup -- the same URL could be recognised by one module and not the other | Files: `phantadex/detection.py`, `tests/test_detection.py`, `CHANGELOG.md`
- **[2026-08-21 18:20:17 +0530]** | Gated discovery's five-second inspection pause on an interactive terminal, and corrected the completion prompt's key hint on Windows, where the read acts on a single keypress rather than a line | Files: `phantadex/discovery/probing.py`, `phantadex/prompt.py`, `CHANGELOG.md`
- **[2026-08-21 18:20:17 +0530]** | Named `pyproject.toml` as the authoritative dependency declaration in `requirements.txt`, and dropped the hard-coded test count from the README, which had drifted | Files: `requirements.txt`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 18:06:56 +0530]** | Dismissed Coursera's daily-goal congratulation dialog, which renders over the item and covers the next-item control, so the advance click landed on the overlay and the run fell back to map navigation on every item; the rule is confined to the dialog because the sidebar carries a 'Today's goals' panel whose text satisfies a page-wide match | Files: `phantadex/modals.py`, `tests/test_modals.py`, `CHANGELOG.md`
- **[2026-08-21 18:06:56 +0530]** | Started a run at the first item the sidebar does not mark complete rather than wherever the tab sits, so resuming a part-finished course no longer walks a skip prompt through every item already done; only rows the sidebar explicitly reports as unfinished are targeted, and `--no-resume` keeps the run on the open item | Files: `phantadex/runner.py`, `phantadex/config.py`, `tests/test_runner.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 18:06:56 +0530]** | Moved media muting from the content handlers to the top of the traversal tick: an item the sidebar already marks complete never reaches a handler, so a reading's narration stayed audible through the course-map scan and the whole 30-second skip prompt | Files: `phantadex/runner.py`, `phantadex/handlers.py`, `tests/test_runner.py`, `CHANGELOG.md`
- **[2026-08-21 17:51:49 +0530]** | Stopped the run stalling on items it cannot complete: the runner idled forever on any page no handler claimed (`LAB` has no handler registered at all), and the graded quiz and assignment handlers blocked in an unbounded wait loop, so a single orientation quiz halted the whole traversal; unclassified pages are now waited out for three passes -- long enough for a page still rendering as `UNKNOWN` -- then stepped past, and graded items advance by default with the new `--pause-on-graded` flag restoring the blocking behaviour. Nothing is answered or submitted in either mode | Files: `phantadex/runner.py`, `phantadex/handlers.py`, `phantadex/config.py`, `tests/test_runner.py`, `tests/test_handlers.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 17:12:40 +0530]** | Muted the narration players some readings autoplay: the mute path queried `video` alone, so a reading's `<audio>` element played aloud for the whole dwell; `interaction.silence_media` now mutes every audio and video element and runs on entry to the reading, discussion and plugin handlers, on every scroll pass (the player is inserted lazily, after the entry pass) and alongside the video mute | Files: `phantadex/interaction.py`, `phantadex/handlers.py`, `phantadex/video.py`, `tests/test_interaction.py`, `tests/fakes.py`, `CHANGELOG.md`
- **[2026-08-21 16:04:11 +0530]** | Pointed the project URLs and clone instructions at the renamed repository (`course-auto` -> `Phantadex`); the old paths still redirect, but a redirect does not satisfy PyPI's trusted-publisher match, which is configured against the repository name | Files: `README.md`, `pyproject.toml`, `CHANGELOG.md`
- **[2026-08-21 15:47:22 +0530]** | Raised the supported Python floor from 3.8 to 3.11, which is the oldest release still receiving security fixes past 2026, and narrowed the CI matrix to 3.11/3.13 on Linux and Windows plus 3.13 on macOS; enabled ruff's `UP` rules for the new floor, which surfaced two locale-encoding defects fixed here -- the packaged selector YAML was read, and the discovered selector state written, in the machine's locale encoding rather than UTF-8 | Files: `pyproject.toml`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `phantadex/schema.py`, `phantadex/discovery/probing.py`, `phantadex/page_ops.py`, `phantadex/storage.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 15:34:03 +0530]** | Made the package installable from an index: moved the Chrome launcher into `phantadex/chrome.py` as `pdex chrome` (with Linux support added and the profile moved to the state directory, since `scripts/` never shipped in the wheel), added `--version`, project metadata and a tag-triggered release workflow using PyPI trusted publishing, and replaced the macos-13 CI leg with Linux legs so the 3.8 floor is covered without an Intel-only image | Files: `phantadex/chrome.py`, `phantadex/cli.py`, `scripts/start_chrome_debug.py`, `pyproject.toml`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `tests/test_chrome.py`, `tests/test_cli.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 15:21:15 +0530]** | Fixed the Windows CI legs, which failed on a test asserting a POSIX literal that `os.path.abspath` resolves against the current drive; corrected the install instructions, which asked for `playwright install chromium` and a second dependency install that the CDP attachment does not need, and documented the launcher's profile directory and the manual Linux launch command | Files: `tests/test_schema.py`, `README.md`, `CHANGELOG.md`
- **[2026-08-21 15:07:55 +0530]** | Fixed two Windows-only failures: console output now writes through a UTF-8 stream, so redirecting a run to a file no longer raises `UnicodeEncodeError` on the first status glyph, and `sanitize_filename` renames the reserved device names (`CON`, `NUL`, `COM1`...) that Windows refuses to create as files | Files: `phantadex/logs.py`, `phantadex/storage.py`, `tests/test_logs.py`, `tests/test_storage.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 21:10:00 +0530]** | Added the `discover` subcommand, so the selector-verification pass runs as `pdex discover` with the shared `--cdp-url` and verbosity flags; it had no CLI entry point and `python -m phantadex.discovery` took no arguments | Files: `phantadex/cli.py`, `tests/test_cli.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 20:35:00 +0530]** | Split `discovery.py` into the package `phantadex/discovery/` (`rules`, `context`, `course_map`, `probing`, `observation`, `state`) behind a re-exporting facade; moved `ELEMENTS_SCHEMA` into the leaf module `phantadex/element_schema.py` so the runtime schema no longer imports the discovery tool; replaced `GLOBAL_REQUIRED_TYPES` and the mapping function's cached attributes with an explicit `ObservationState`; routed the sidebar hop through `urls.absolute_url` and the course-tab check through `urls.PLATFORM_HOST`; moved the `NODE_OPTIONS` side effect into `session.quiet_node_driver()` | Files: `phantadex/discovery/`, `phantadex/element_schema.py`, `phantadex/schema.py`, `phantadex/session.py`, `phantadex/urls.py`, `tests/test_discovery.py`, `tests/test_review_regressions.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 13:00:08 +0530]** | Added `-v`/`--verbose` and `-q`/`--quiet` shorthands, with `--log-level` still winning when set explicitly; logged the actions that were previously invisible at every level -- each successful click with the label of the control it hit, each scroll's distance, the play path taken, resumes, seeks, idle cursor moves and the archive and home navigations; and routed discovery's 36 raw `print` calls through the logger so its output honours the log level | Files: `phantadex/config.py`, `phantadex/interaction.py`, `phantadex/video.py`, `phantadex/handlers.py`, `phantadex/archive.py`, `phantadex/session.py`, `phantadex/discovery.py`, `tests/test_cli.py`, `tests/test_interaction.py`, `tests/test_discovery.py`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 03:41:12 +0530]** | Made the click jitter real by passing it as a `position` to hover and click, which previously moved the cursor to an offset point and still clicked the element's exact centre; turned off forced clicking by default so a click swallowed by an overlay is reported as a failure rather than a success, with a 5-second bound on the actionability wait; and dropped the synthetic `timeupdate` from the seek script, the only script-made event in the package, since assigning `currentTime` already fires the browser's own seeking, seeked and timeupdate | Files: `phantadex/interaction.py`, `phantadex/video.py`, `tests/fakes.py`, `tests/test_interaction.py`, `tests/test_review_regressions.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 03:05:40 +0530]** | Confined the in-video interrupt rules (Reflect, Poll, Question) to the player's dialog and `rc-VideoQuiz` container instead of falling back to the page, so an ordinary heading containing "Poll" or "Question" can no longer satisfy a modal rule; the page fallback now serves only the Honor Code and demographics interstitials | Files: `phantadex/modals.py`, `tests/test_modals.py`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 02:35:10 +0530]** | Anchored item identity on the course slug and a known item segment so week pages and same-id items in different courses no longer compare equal; gave the URL segment precedence over an embedded player when classifying a page; derived the scanner's segment table from the classifier's so the two cannot drift; restored verified-selector priority by scanning candidates in declaration order instead of one DOM-ordered joined locator; advanced past a cleared graded quiz instead of re-entering it every tick; quarantined an unparseable ledger rather than rebuilding over it; archived skipped discussions; kept the failure count on an item that cannot be advanced past; preferred dialog scope and a heading-local button container when dismissing modals; seeded selector discovery from learned state alone so packaged defaults are no longer frozen into the state file; and made a zero-delta scroll a no-op | Files: `phantadex/urls.py`, `phantadex/detection.py`, `phantadex/discovery.py`, `phantadex/navigation.py`, `phantadex/schema.py`, `phantadex/handlers.py`, `phantadex/course_manager.py`, `phantadex/runner.py`, `phantadex/modals.py`, `phantadex/interaction.py`, `tests/**`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-21 00:52:35 +0530]** | Moved discovery-verified selectors out of package data into an atomically written user state file layered over shipped defaults; replaced fuzzy archive dedupe with exact content matching so the saved file and the XML ledger always agree; made `--video-threshold` the actual watch target with 0-100 validation and a frozen-playback bound; distinguished an unmapped item from the end of the course; scoped locked-item detection so the ordinary pager is not mistaken for a lock; recorded persistent handler failures in the ledger; keyed course identity on the URL slug; made discussions archivable; scoped sidebar module headers to the outline and merged same-titled modules; recognised `ungradedLti` as a lab; and adopted a Ruff configuration with a CI lint job | Files: `phantadex/schema.py`, `phantadex/discovery.py`, `phantadex/storage.py`, `phantadex/course_manager.py`, `phantadex/handlers.py`, `phantadex/config.py`, `phantadex/interaction.py`, `phantadex/navigation.py`, `phantadex/page_ops.py`, `phantadex/runner.py`, `phantadex/urls.py`, `pyproject.toml`, `.github/workflows/ci.yml`, `tests/**`, `README.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 23:59:29 +0530]** | Scoped reading scroll discovery to content ancestors, stopped movement at the bottom, ended reading sessions on item navigation, and rejected transient course-name placeholders | Files: `phantadex/interaction.py`, `phantadex/handlers.py`, `phantadex/discovery.py`, `phantadex/archive.py`, `tests/test_interaction.py`, `tests/test_handlers.py`, `tests/test_discovery.py`, `tests/test_branding.py`, `README.md`, `PHANTADEX_ARCHITECTURE_REVIEW.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 23:30:59 +0530]** | Unified Archive/Dex terminal rendering with compact rows, explicit course states, and TTY map spinners; migrated archive roots and ledgers to `phantadex_archive/<course>/<slug>.pdex.xml` without overwrites; recovered Watch from locked/missing panes, paused required assignments without cycling, and cleaned CDP interruption shutdown | Files: `phantadex/logs.py`, `phantadex/archive.py`, `phantadex/cli.py`, `phantadex/discovery.py`, `phantadex/config.py`, `phantadex/urls.py`, `phantadex/course_manager.py`, `phantadex/page_ops.py`, `phantadex/navigation.py`, `phantadex/runner.py`, `phantadex/handlers.py`, `phantadex/session.py`, `phantadex/watch.py`, `phantadex/config.yaml`, `tests/**`, `.gitignore`, `README.md`, `PHANTADEX_ARCHITECTURE_REVIEW.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 22:45:19 +0530]** | Routed transcript, modal, discovery navigation, and sidebar expansion controls through the shared randomized pointer and reaction-delay click path, with regression coverage | Files: `phantadex/page_ops.py`, `phantadex/modals.py`, `phantadex/discovery.py`, `tests/fakes.py`, `tests/test_modals.py`, `tests/test_page_ops.py`, `tests/test_discovery.py`, `README.md`, `COURSERA_ARCHITECTURE_REVIEW.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 22:37:20 +0530]** | Added packaged verified selectors, bounded handler retries, shared reading/video control clicks, CDP cleanup, macOS/Windows Chrome launch support, bounded dependencies, and cross-platform CI | Files: `phantadex/config.yaml`, `phantadex/schema.py`, `phantadex/session.py`, `phantadex/runner.py`, `phantadex/navigation.py`, `phantadex/video.py`, `scripts/start_chrome_debug.py`, `.github/workflows/ci.yml`, `pyproject.toml`, `requirements.txt`, `tests/test_schema.py`, `tests/test_branding.py`, `tests/test_runner.py`, `tests/test_navigation.py`, `README.md`, `COURSERA_ARCHITECTURE_REVIEW.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 21:35:55 +0530]** | Fixed cross-row completion false positives, verified live reading completion, and added completed/incomplete/unknown markers to the Dex tree without changing course-map tuples | Files: `phantadex/page_ops.py`, `phantadex/discovery.py`, `phantadex/cli.py`, `tests/test_page_ops.py`, `tests/test_discovery.py`, `tests/test_navigation.py`, `tests/test_cli.py`, `README.md`, `COURSERA_ARCHITECTURE_REVIEW.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 21:01:44 +0530]** | Restored `--reading-minutes MIN[-MAX]` for fixed or randomized fallback reading dwell, with positive ordered validation and startup visibility | Files: `phantadex/config.py`, `tests/test_cli.py`, `README.md`, `COURSERA_ARCHITECTURE_REVIEW.md`, `HANDOVER.md`, `CHANGELOG.md`
- **[2026-08-20 17:24:11 +0530]** | Restored live TXT transcript fallback for Coursera's Files panel, prevented open-panel toggle failures, added cross-process course locking with atomic XML replacement on POSIX/Windows, and covered browser discovery orchestration | Files: phantadex/: discovery.py, page_ops.py, course_manager.py; tests/: test_discovery.py, test_page_ops.py, test_course_manager.py; config.yaml, README.md, COURSERA_ARCHITECTURE_REVIEW.md, HANDOVER.md, CHANGELOG.md
- **[2026-08-20 17:00:31 +0530]** | Added installable phantadex/pdex CLI aliases with default Dex tree, explicit Skip/Watch/Archive modes, package execution (__main__.py), and backwards-compatibility wrappers (Skip reuses randomized 97.5-98.5% seek; Watch defaults to 100% completion) | Files: phantadex/: cli.py, __main__.py, watch.py, archive.py; tests/: test_cli.py, test_branding.py; pyproject.toml, phantadex_watch.py, phantadex_archive.py, README.md, CONTRIBUTING.md, COURSERA_ARCHITECTURE_REVIEW.md, HANDOVER.md
- **[2026-08-20 16:17:42 +0530]** | Rebranded package and entry points as Phantadex, wiring Watch, Archive, Dex, and Link console identities while preserving Coursera archive directory paths | Files: phantadex/**; tests/**; phantadex_watch.py, phantadex_archive.py, run_tests.py, README.md, CONTRIBUTING.md, COURSERA_ARCHITECTURE_REVIEW.md, HANDOVER.md
- **[2026-08-20]** | Restructured into the `coursera/` package with a `Runner`, per-content handlers, unified `detection.classify()` URL-segment classification, shared navigation, and synchronized `config.yaml`/fallback selector resolution | Files: `coursera/runner.py`, `coursera/handlers.py`, `coursera/detection.py`, `coursera/navigation.py`, `coursera/schema.py`, `coursera/discovery.py`, `coursera/config.py`, `config.yaml`
- **[2026-08-20]** | Updated archive persistence to disambiguate unmapped items with opaque IDs, preserve distinct content versions, reconcile the XML ledger on load, and normalize transcript/reading text while retaining cue timestamps | Files: `coursera/course_manager.py`, `coursera/storage.py`, `coursera/text.py`; `tests/**`
- **[2026-08-20]** | Fixed traversal failures in modal handling, reading-container scrolling, non-interactive stdin prompts, and unverified Next-button clicks | Files: `coursera/modals.py`, `coursera/interaction.py`, `coursera/prompt.py`, `coursera/navigation.py`; `tests/**`
- **[2026-08-20]** | Added 174 offline Playwright-fake tests, configurable CDP/archive/log/video CLI options, connection diagnostics, terminal glyph logs with `NO_COLOR` support, and browser-profile ignore rules | Files: `tests/**`, `run_tests.py`, `coursera/config.py`, `coursera/session.py`, `coursera/logs.py`, `.gitignore`, `requirements.txt`
- **[2026-04-26 00:21:57 IST]** | Added direct video skip range support (VIDEO_DIRECT_SKIP_RANGE / --video-skip-range, defaulting to randomized 97.5-98.5% seek and 100%/ended completion), fixed fallback transcript directory creation, normalized item path comparisons, prioritized course-map navigation order, updated config.yaml selectors via discovery, and added PyYAML dependency | Files: coursera_stealth.py, config.yaml, requirements.txt, CHANGELOG.md
- **[2026-02-03 (v2.5.0)]** | Added the `run_tests.py` offline verification suite, isolated test artifacts, and playback/timing checks | Files: `run_tests.py`, `coursera_stealth.py`
- **[2026-02-03 (v2.0.0)]** | Added multi-step pointer movement, double-click guards, modal-aware scrolling, and automatic reload after repeated stalls | Files: `coursera_stealth.py`
- **[2026-02-03 (v1.5.0)]** | Added pointer and scroll trajectories, WPM-based reading sessions with content-bottom detection, and a 20-iteration stuck threshold | Files: `coursera_stealth.py`
- **[2026-02-03 (v1.4.2)]** | Added browser reconnection, guarded handler failures, visible-first Next selection, bounded pointer movement, and Peer/Honors assessment detection | Files: `coursera_stealth.py`
- **[2026-02-02 (v1.4.1)]** | Added two-stage transcript extraction with a Downloads `.txt` fallback and corresponding discovery selectors | Files: `coursera_stealth.py`, `discover_selectors_coursera.py`
- **[2026-02-02 (v1.4.0)]** | Added randomized video completion targets, playback/reading pointer movement, and randomized click reaction intervals | Files: `coursera_stealth.py`
- **[2026-02-02 (v1.3.0)]** | Added the standalone map-driven `coursera_archiver.py` with XML-ledger resumption | Files: `coursera_archiver.py`, `coursera_stealth.py`
- **[2026-02-02 (v1.1.0)]** | Added course-map scanning, item-type extraction, URL-segment prioritization, exit-target discovery, and non-essential item filtering | Files: `coursera_stealth.py`
- **[2026-01-25 (v1.0.1)]** | Added a 30-second buffering timeout and URL-based stuck counter | Files: `coursera_stealth.py`
- **[2026-01-25 (v1.0.0)]** | Added CDP attachment, native-ended video completion, reading scrolling, versioned text extraction, local visit history, graded-quiz pause, and ungraded-item skip behavior | Files: `coursera_stealth.py`, `visited_history.json`, `README.md`
